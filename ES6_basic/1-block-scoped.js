export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    const tisket = true;
    const tasket = false;
    return [tisket, tasket]
  }

  return [task, task2];
}
